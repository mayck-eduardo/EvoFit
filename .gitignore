# GitHub Copilot
# .gitignore recomendado para projeto React Native + Expo
# Ajuste conforme necessidades do projeto (ex.: commit de arquivos de configuração compartilhada)

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pnpm-lock.yaml   # opcional: remova se você quiser commitar o lockfile do pnpm

# Expo
.expo/
expo-env.d.ts
.expo-shared/local*      # arquivos locais gerados pelo Expo (mantenha .expo-shared se quiser compartilhar configurações)
web-build/

# Environment / secrets
.env
.env.*
.env.*.local

# iOS
ios/Pods/
ios/build/
DerivedData/
*.xcuserstate
*.moved-aside
*.xcworkspace/xcuserdata/

# Android
android/.gradle/
android/app/build/
android/build/
local.properties
*.keystore
*.jks

# Build / caches
.cache/
tmp/
*.log
coverage/
dist/
!.github/                   # permite manter arquivos de workflow

# Editor / IDE
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
*.iml

# macOS
.DS_Store

# Fastlane / CI artifacts
fastlane/report.xml
fastlane/Preview.html

# Credentials / signing
*.p12
*.pem
*.mobileprovision
google-services.json
GoogleService-Info.plist
credentials.json

# Misc
.pnpm-debug.log
.pnp.*
.parcel-cache/

# Nota: mantenha lockfiles como package-lock.json ou yarn.lock se desejar garantir reprodutibilidade.